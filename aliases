alias a!='alias | grep '

alias rmsc='rm screenshot*'

alias gdt='git difftool'
alias gdts='gdt --staged'
alias gs='git status'
alias gcam='git commit -am '
alias gca='git commit --amend'
alias gi='git add -i'
alias gco='git checkout'

function gsa() {
  git stash apply stash@{$1}
}

function envpass() { read -s $1; export $1; }
# AWS Console Aliases
alias awsc="source ~/aws/bin/activate"
alias awsd="deactivate"
alias awsl="okta-aws default"
alias awsecr="aws ecr get-login --registry-ids 674907502808 --region us-east-1 --no-include-email"
# alias awslecs="eval $(aws ecr get-login --registry-ids 674907502808 --region us-east-1 --no-include-email)"
